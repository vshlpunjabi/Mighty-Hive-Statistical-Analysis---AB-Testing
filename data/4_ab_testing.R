conversion_data = read.csv('landing_page_data.csv', header = T)
head(conversion_data)
sum(conversion_data$a[conversion_data$a == 1],na.rm = TRUE)
sum(conversion_data$b[conversion_data$b == 1])
a_obs = na.omit(conversion_data$a)
b_obs = na.omit(conversion_data$b)
a_proportion = sum(a_obs)/length(a_obs)
b_proportion = sum(b_obs)/length(b_obs)
p=(sum(a_obs)+sum(b_obs))/(length(a_obs)+length(b_obs))
se=sqrt(p*(1-p)*(1/length(a_obs)+1/length(b_obs)))
t=b_proportion-a_proportion-0
z = round(t/se,3)
round((b_proportion-a_proportion-0)/sqrt(p*(1-p)*(1/length(a_obs)+1/length(b_obs))),3)
t.test(b_obs,a_obs,alternative = "two.sided",paired = FALSE)
